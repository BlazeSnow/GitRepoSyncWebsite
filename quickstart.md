# 快速开始

## 安装

从 [GitHub Releases](https://github.com/BlazeSnow/GitRepoSync/releases/latest) 下载对应平台的安装包即可：

| 平台                | 安装包                                       |
| ------------------- | -------------------------------------------- |
| Windows x64         | `GitRepoSync_<版本>_x64-setup.exe` 或 `.msi` |
| macOS Apple Silicon | `GitRepoSync_<版本>_aarch64.dmg`             |
| macOS Intel         | `GitRepoSync_<版本>_x64.dmg`                 |
| Linux x64           | `.deb` / `.rpm` / `.AppImage`                |

`vX.Y.Z-beta.N` 形式的版本为测试版（beta），功能更新更频繁，但可能存在不稳定因素。

## 登录

系统自带初始账号：

- 用户名：`admin`
- 密码：`admin123`

首次启动自动创建。可勾选「保持登录 30 天」，重启软件后自动恢复登录态；登录后请尽快在设置页修改密码。

## 设置基地址

基地址是本地中转目录的父目录（默认为本机用户目录下的 `repo` 文件夹）：

1. 进入「设置」页
2. 通过「选择目录…」调用系统目录选择器，选中即保存

所有仓库都会以 `{基地址}/{仓库名}` 的形式存放在该目录下，作为同步的中转站。

## 放入仓库

把要备份的仓库 clone 或移动到基地址目录下，软件启动时会**自动发现并登记**：

- `origin` 远端作为**源地址**
- **其余全部远端自动登记为备份目标**（gitee / gitlab / 自命名均可，1 对多）
- 没有任何备份远端时标记为「未配置」，配置后即可参与同步

之后在基地址内新增 / 删除远端，备份列表也会自动跟随，无需手工维护。

也可以在「添加仓库」中手动录入仓库与备份目标，不依赖基地址内的目录。

## 开始同步

在「同步仓库」页选择同步范围（全部 / 1 / 3 / 7 / 30 天内未同步），点击「开始同步」：

- 一次同步依次推送到全部备份目标，每个目标独立显示状态
- 单个目标失败不影响其余目标
- 同步期间可点击「停止同步」终止运行中与排队中的同步

表格支持双击或右键编辑仓库，右键菜单还提供「立即同步」与「删除」。

## 下一步

- 了解[同步原理](/sync)：三步流水线、LFS 与 submodule 的处理方式
- 通过 [Agent 接入（MCP）](/mcp)让 AI Agent 管理仓库并触发同步
