# 常见问题

## 提示 git 命令失败？

确认系统已安装 [Git](https://git-scm.com) 并加入 PATH；私有仓库需提前配置好凭据（HTTPS 凭据管理器或 SSH 免密）。

软件同步期间会禁用 git 交互式凭据输入（`GIT_TERMINAL_PROMPT=0`），避免无人值守时卡住，因此凭据必须在同步前配置好。

## LFS 文件没有备份？

安装 [git-lfs](https://git-lfs.com) 后重新同步。LFS 对象在推送前会预上传到每个目标（`git lfs push --all`），若目标行出现「LFS 对象预上传失败」警告，通常是目标侧 LFS 认证问题（SSH 目标走 HTTPS LFS 端点），请配置凭据助手或改用 HTTPS 地址。

详见[同步原理](/sync)。

## 仓库显示「未配置」？

该仓库缺少 `origin` 源地址，或没有任何备份目标远端。补齐后即可参与同步：

- 未配置仓库不参与同步（「开始同步」自动跳过、MCP `sync_repo` 直接拒绝），避免必然的「失败」记录
- 在基地址内给仓库添加远端（`git remote add <名字> <地址>`），软件会自动登记为备份目标

## 删除仓库会删除本地目录吗？

不会。删除是**软删除**：隐藏条目并清空目标配置，基地址内的目录原样保留；同时该目录也不会被自动发现重新登记。想重新登记可以在「同步仓库」页点击「刷新仓库」前先恢复配置，或在「添加仓库」中按同名重建（合并语义）。

## 「保持登录 30 天」是什么原理？

勾选后会话令牌写入数据库，软件重启后自动恢复登录态；不勾选则关闭软件即失效。
